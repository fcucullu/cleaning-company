---
import { FaHome, FaRegCalendarAlt, FaRegBuilding, FaHandsHelping } from 'react-icons/fa';

const features = [
  { icon: FaRegCalendarAlt, title: 'Limpieza de propiedades vacacionales' },
  { icon: FaHome, title: 'Limpieza de urbanizaciones' },
  { icon: FaRegBuilding, title: 'Limpieza profunda para unidades recién construidas' },
  { icon: FaHandsHelping, title: 'Gestión y mantenimiento completo de propiedades' },
];

---

<section class="bg-blue-700 py-20 text-white text-center">
  <div class="container mx-auto px-4">
    <h2 class="text-4xl font-serif font-bold mb-4">Más de 5 años de experiencia en la Costa del Sol</h2>
    <p class="text-xl mb-12 max-w-2xl mx-auto">
      Nuestra reputación se basa en un servicio de alta calidad, garantía de satisfacción y un enfoque personalizado para cada propiedad.
    </p>

    <div class="bg-white text-blue-700 rounded-lg shadow-lg p-8">
      <div class="grid grid-cols-1 md:grid-cols-3 lg:grid-cols-4 gap-8">
        {features.map((feature) => (
          <div class="flex flex-col items-center">
            <feature.icon className="w-16 h-16 mb-4" />
            <p class="text-sm font-semibold">{feature.title}</p>
          </div>
        ))}
      </div>
    </div>

    <p class="text-xl mt-12 max-w-2xl mx-auto">
      Si no está completamente satisfecho con nuestro servicio, ¡es <strong>completamente gratis!</strong>
    </p>
  </div>
</section>
