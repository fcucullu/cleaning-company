---
import { FaHome, FaRegCalendarAlt, FaRegBuilding, FaHandsHelping } from 'react-icons/fa';

const features = [
  { icon: FaRegCalendarAlt, title: 'Vacation property cleaning' },
  { icon: FaHome, title: 'Communities cleaning' },
  { icon: FaRegBuilding, title: 'Deep cleaning for newly constructed units' },
  { icon: FaHandsHelping, title: 'Full property management and maintenance' },
];

---

<section class="bg-blue-700 py-20 text-white text-center">
  <div class="container mx-auto px-4">
    <h2 class="text-4xl font-serif font-bold mb-4">Over 5 years of experience on the Costa del Sol</h2>
    <p class="text-xl mb-12 max-w-2xl mx-auto">
      Our reputation rests on high-quality service, a satisfaction guarantee, and a personalized approach to every property.
    </p>

    <div class="bg-white text-blue-700 rounded-lg shadow-lg p-8">
      <div class="grid grid-cols-1 md:grid-cols-3 lg:grid-cols-4 gap-8">
        {features.map((feature) => (
          <div class="flex flex-col items-center">
            <feature.icon className="w-16 h-16 mb-4" />
            <p class="text-sm font-semibold">{feature.title}</p>
          </div>
        ))}
      </div>
    </div>

    <p class="text-xl mt-12 max-w-2xl mx-auto">
      If you're not fully satisfied with our service, it’s <strong>completely free!</strong>
    </p>
  </div>
</section>
